---
- name: user
  hosts: demo
  tasks:
    - name: Preconfig block
      block:
        - name: Create user
          vars:
            user: user1
          user:
            name: "{{ user }}"
            state: present
          register: error
        - name: install curl
          apt:
            name: curl
            update-cache: yes
          register: error
      become: true
      rescue:
        - name: Some error print
          debug:
            var: error
      always:
        - name: Reboot
          debug:
            msg: "Reboooooting"